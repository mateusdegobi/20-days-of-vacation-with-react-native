import React, { forwardRef } from "react";
import { Input } from "./styles";

function TextInputOutline(props) {
  return <Input placeholder="Digite..." {...props} />;
}

export default TextInputOutline;
