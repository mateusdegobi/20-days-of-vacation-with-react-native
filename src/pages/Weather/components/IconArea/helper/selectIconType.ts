import { NameIconsTypes } from "../../../../../constants/icons";

function selectIconType(type: string): NameIconsTypes {
  if (type === "") {
  }

  return "NeveWeather";
}

export default selectIconType;
